const [a, b, c, d] = [3, 5, 1, 15] // criando constantes 'a', 'b', 'c', 'd' através do operador destructuring
// a = 3; b = 5; c = 1; d = 15

const soma = a + b + c + d
const subtracao = d - b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2

console.log(soma, subtracao, multiplicacao, divisao, modulo)
